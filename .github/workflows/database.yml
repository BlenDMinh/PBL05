name: Reset database
on:
  workflow_dispatch: {}
  push:
    paths:
      - "database/**"
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Reset database
        run: cd database && npm run db:reset
